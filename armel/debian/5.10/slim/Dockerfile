# AUTOGENERATED FILE
FROM resin/armel-debian:jessie

ENV NODE_VERSION 5.10.0

RUN apt-get update && apt-get install ca-certificates --no-install-recommends \
	&& rm -rf /var/lib/apt/lists/*

RUN buildDeps='curl' \
	&& set -x \
	&& apt-get update && apt-get install -y $buildDeps --no-install-recommends \
	&& rm -rf /var/lib/apt/lists/* \
	&& curl -SLO "http://resin-packages.s3.amazonaws.com/node/v$NODE_VERSION/node-v$NODE_VERSION-linux-armel.tar.gz" \
	&& echo "dd8581408cd2d69313ee52af0bc13c4e03aa558670374b1b6cd22e8ccadee1e6  node-v5.10.0-linux-armel.tar.gz" | sha256sum -c - \
	&& tar -xzf "node-v$NODE_VERSION-linux-armel.tar.gz" -C /usr/local --strip-components=1 \
	&& rm "node-v$NODE_VERSION-linux-armel.tar.gz" \
	&& apt-get purge -y --auto-remove $buildDeps \
	&& npm config set unsafe-perm true -g --unsafe-perm \
	&& rm -rf /tmp/*
